import Vue from 'vue'
import Vuex from 'vuex'
import listings from './modules/listings'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    listings
  }
})
